{
  "name": "autoqueuer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "spicetify-creator",
    "build-local": "spicetify-creator --out=dist --minify && cp -r dist/* /Users/alemangerman/.config/spicetify/CustomApps/AutoQueuer/",
    "deploy": "spicetify-creator && spicetify apply",
    "watch": "spicetify-creator --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "spicetify-creator": "^1.0.17"
  }
}
